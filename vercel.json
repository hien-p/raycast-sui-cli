{
  "buildCommand": "cd sui-cli-web && npm install && npm run build",
  "outputDirectory": "sui-cli-web/packages/client/dist",
  "installCommand": "npm install --prefix sui-cli-web",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
